# Benchmark -- S3 ViRGE 86C375 + Pentium OverDrive 83, merged fix build

Closing cell of the ViRGE CPU-tier sweep -- all four CPU tiers
(486DX2-50, 486DX2-66, Am5x86-133, Pentium OverDrive 83) now have a
ViRGE datum on the merged fix build, matching the Mach64 sweep's
coverage. CPU-only swap -- ViRGE stays installed, so no UVCONFIG re-run
was needed. Driven on the rig by `vcctrl-c3`; coordinated/recorded by
this session.

## Hardware
CPU: Intel Pentium OverDrive, ~83MHz, CPU Features: FPU TSC MSR CX8 --
  confirmed via fresh `dinspect` scan, matching the three-way
  confirmation (fixed dinspect, independent CPUID dump, PhoenixBIOS POST
  text via the hardware camera) from the original
  `docs/benchmarks/mach64-215ct-pod83-2026-09-03.md` leg. This chip has
  a documented RDTSC-fault-under-EMM386 hazard once an earlier
  TSC-calibration hang was fixed (same doc); this scan was watched
  through the specific phase that used to hang and completed in ~24s
  with no stall -- the PIT-fallback fix is holding on this rig.
Clock: ~83MHz
L1: (not measured)
L2: (not measured)
RAM: 48 MB
Video: S3 ViRGE/DX or /GX (86C375), 4 MB VRAM -- unchanged from the
  prior three ViRGE legs today (card did not move). Fresh scan confirms:
  Video Chipset = "S3 ViRGE/DX or /GX", Video Memory = 4 MB, VBE string
  still "Universal VESA VBE 6.70 (VBE 3.0)" (UniVBE's shim).
Sound: PicoGUS v2, Sound Blaster mode (unchanged)
MIDI: n/a
DOS: MS-DOS 6.22
VESA: UniVBE 6.70, VBE 3.0, LFB (`has_lfb=1 use_lfb=1 banked=0`)

Hardware ID: HW-POD83

## Build
Commit: same staged build as the three prior ViRGE legs today -- reused
  byte-identical, not rebuilt.
Upstream revision: Passage `2f713f261dc907f6feda106ebbee0464eeab791d`,
  minorGems `ef42b1ce511f2d355d5fc898fcce0b0af3a76d62` (unchanged all
  campaign)
SDL revision: `74a746281f2208e07a7680560fcb7ec57565228e` (unchanged)
shared/ (.sdl-dos-ports/) pin: unchanged
Optimization flags: unchanged
`build_sha12 = a5e9835f12e7` (the merged-fix pin, same as every ViRGE
  leg today)
`AUDIO_TIER=high` (default), `SONG.WAV` tier-matched.
`sha256(exe) = 8df054c6930187262c732b9b773a81e8838e2e9f6c5aada5dd964b7f6c9b2d6a`
  -- re-verified against the already-staged `C:\DOSSAGE` tree (unchanged
  file size/timestamp; not re-transferred).

## Scene
Game: Passage
Level/room: n/a
Save file: n/a
Duration: 298s (life 1, natural completion, reached the end-of-life
  gravestone marker), plus the established trailing life (11s,
  two-Escape exit) -- not counted toward the fps figures below.

## Results
Average FPS: **15.016779** (4475 frames / 298s, `Game time = 4:58`,
  captured verbatim from `FPS.TXT`). **PASS.** Frame-time samples:
  "4474 used, 0 rejected any category" -- clean.

  **Fifth appearance of this exact 15.016779fps/4475-frame signature**
  across the campaign (486DX2-50+ViRGE, 486DX2-66+ViRGE,
  Am5x86-133+ViRGE, and the original Am5x86-133+Mach64 datum, now
  POD83+ViRGE) -- at this point fully established, not merely
  suggestive: any uninterrupted natural life on this build hits exactly
  this figure whenever the CPU has enough pacer slack to sit flat
  against the 15fps ceiling, independent of both CPU tier and video
  card.
1% low: RUNMANIFEST `fps_p50=15.00`, `fps_p95=14.97`. Completes a
  monotonic `fps_p95` progression with CPU speed across the whole ViRGE
  sweep: 14.80 (486DX2-50) -> 14.94 (486DX2-66) -> 14.96/14.97
  (Am5x86-133) -> 14.97 (POD83, this leg). The trailing (11s) life hit
  `fps_p95=15.00` exactly -- the tightest margin of any leg, consistent
  with this being the fastest CPU measured against ViRGE.
Simulation rate: locked at the 15fps design ceiling, pacer-bound.
Audio underruns: not instrumented. `AUDIO_PRESENT` confirmed multiple
  times mid-run. Tier-matched build, no pitch/tempo mismatch to caveat.
Peak memory: not measured.
Notes:
- **ViRGE CPU-tier sweep CLOSED.** All four CPU tiers on the port's
  hardware roster now have a ViRGE datum on the current merged build,
  matching the coverage the Mach64 sweep already had. See
  `docs/BENCHMARK-PLAN.md`'s ViRGE CPU-tier expansion table for the
  full set: `docs/benchmarks/virge-86c375-486dx2-50-2026-09-04.md`,
  `...-486dx2-66-2026-09-04.md`, `...-am5x86-2026-09-04.md`, and this
  file.
- **RDTSC/EMM386 hazard did not recur.** The original POD83+Mach64 leg
  found this chip faults on RDTSC under EMM386 once a separate
  TSC-calibration hang was fixed -- confirmed here that the fixed
  `dinspect` build handles it (PIT-fallback path), same as the earlier
  leg, not a fresh finding but worth reconfirming per-swap rather than
  assuming a fix holds forever.
- **No repeat of the original POD83 leg's "second life Escape didn't
  register" quirk** -- this run's two-Escape exit worked cleanly on the
  first try.
- Staging reused, not re-transferred: confirmed via unchanged file
  size/timestamp against the prior (Am5x86-133) leg before relying on
  it.
- Visual: matched `docs/screenshots/dossage-gameplay.png` throughout, no
  corruption; this CPU reached visible gameplay fastest of the four
  ViRGE tiers (past mode-switch warmup by ~40s). Clean two-Escape exit
  confirmed via the hardware camera.
- `SDLDBG.LOG`: identical mode-set to every ViRGE leg today --
  `DOSVESA-CTRL: VBE 3.0 total_vram=4096_KB`,
  `oem_string='Universal VESA VBE 6.70'`,
  `oem_vendor='SciTech Software, Inc.'`; `cirrus-lfb-aperture-bug: NOT
  detected -> LFB path remains enabled`; `DOSVESA-MODESET: id=0x01D9
  320x240 16bpp has_lfb=1 use_lfb=1 banked=0 lfb_addr=0x78000000`.
- Wall-clock bracket (control-host UTC): launch 1788549182.14, title
  reappeared 1788549525.15 (delta 343.0s vs. 298s reported life --
  ~45s launch/warmup slop, same documented pattern as every other leg).

## Measurement method
Real hardware via vcctrl (authoritative), Gateway 2000 rig,
`gateway2000` profile. Single run, first datum for this CPU/card
combination on the merged fix build -- not yet a repeatability claim.
This closes the ViRGE CPU-tier sweep at four-of-four CPUs measured.
