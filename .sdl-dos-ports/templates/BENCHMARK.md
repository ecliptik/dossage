<!--
Template: ports/<name>/benchmarks/<hardware>-<date>.md in a port repo,
or gallery/<name>/benchmarks/ once published to the hub. One file per
benchmark run. Prefer a repeatable scene (scripted/TAS input for action
games) so results are comparable run over run.
-->

# Benchmark

## Hardware
CPU:
Clock:
L1:
L2:
RAM:
Video:
Sound:
MIDI:
DOS:
VESA:

<!-- Use a HARDWARE.md ID (HW-486-50, HW-486-66, HW-POD83, HW-5X86, HW-P75)
     if this matches the reference matrix. -->
Hardware ID:

## Build
Commit:
Upstream revision:
SDL revision:
shared/ (.sdl-dos-ports/) pin:
Optimization flags:

## Scene
Game:
Level/room:
Save file:
Duration:

## Results
Average FPS:
1% low:
Simulation rate:
Audio underruns:
Peak memory:
Notes:

## Measurement method
<!-- real hardware via vcctrl (authoritative) vs. DOSBox-X/86Box
     (automation gate only) -- state which, and if real hardware, how many
     repeat runs this reflects (see docs/hardware-testing.md on
     repeatability -- a single run is not a result). -->
