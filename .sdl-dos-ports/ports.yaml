# sdl-dos-ports candidate backlog and status tracker.
#
# Each port lives in its own git repository. This file is the queue: a
# candidate starts at dos_status BACKLOG with no port_repo_url. Claiming a
# candidate means running scripts/new-port.sh, which scaffolds the new
# port's repo (wired to this repo's shared/ via git submodule), and then
# updating that candidate's entry here (dos_status -> RESEARCH,
# port_repo_url set) in a commit to this repo.
#
# dos_status ladder:
#   BACKLOG -> RESEARCH -> BLOCKED -> BOOTSTRAP -> COMPILES -> STARTS ->
#   TITLE_SCREEN -> PLAYABLE -> FULL_GAME -> OPTIMIZING -> RELEASE_READY
#
# difficulty: 1 = trivial, 2 = easy, 3 = moderate, 4 = difficult,
#             5 = major engine-port effort
#
# Never guess an upstream_url. If a candidate's upstream is not yet located,
# leave upstream_url null and say so in notes -- locating and pinning the
# canonical upstream revision is that candidate's literal first task.
#
# upstream_license / upstream_license_verified: same rule applies to
# licensing as to upstream_url -- NEVER GUESS. upstream_license_verified is
# only true once someone has actually read the upstream project's own
# LICENSE/COPYING file (or equivalent) in the pinned revision and recorded
# what it says. A "commonly believed to be X" recollection is not
# verification -- leave upstream_license null and put the lead in notes
# instead. We need this to be right because it governs what we are legally
# allowed to distribute, modify, and how (see docs/licensing.md and
# CLAUDE.md's "Never contribute upstream" section for the related policy on
# not sending anything back to these upstreams).

ports:
  - name: doskutsu
    upstream_url: https://github.com/nxengine/nxengine-evo
    upstream_revision: 1f093d1423cc395eb199230cd609b806ef1daa36
    upstream_license: GPL-3.0
    upstream_license_verified: true
    port_repo_url: https://github.com/ecliptik/doskutsu
    language: C++
    original_sdl_version: "2 (via nxengine-evo)"
    target_sdl_version: "3"
    graphics_api: software / VESA
    dos_status: RELEASE_READY
    priority: 0
    difficulty: null
    dos_minimum_target: 486DX2-66
    dos_recommended_target: Pentium 75
    maintainer: ecliptik
    notes: >
      Reference port. Cave Story via NXEngine-evo (GPLv3) on
      SDL3/DJGPP/CWSDPMI. Source of the shared/ platform layer.
      shared/ submodule adoption (plans/DOSKUTSU-MIGRATION-PLAN.md)
      LANDED on doskutsu main, currently at 332d379 (pin bump from
      f00dd84 to pick up the hub's shared/skills/; pushed to Forgejo
      origin). Submodule (.sdl-dos-ports) pinned at sdl-dos-ports
      006a02c75a3cd8279a735b34084fbd94cff99596. Steps 1-4 landed:
      patches/SDL + patches/SDL_mixer symlinked into
      shared/patches/sdl3-dos + sdl3-mixer, the 4 vendor scripts
      symlinked into shared/scripts/, and the SDL3/mixer/image build
      stages consumed via shared/build/sdl3-dos.mk. Build-correctness
      verified: verify-patches-applied 114+2 patches exit 0 through
      the symlinks, make all exit 0 (rebuilt binary byte-identical to
      a from-scratch build on the same host), make smoke gate PASS,
      and a real-hardware RUNMANIFEST run (g2k rig) confirmed
      environment=realhw + build_sha12=04388290f735. Performance
      confirmed separately post-migration: a real-hardware ABBA
      reproduction of patch 0328's (BG blank-tile-skip) measured KPI
      on the POD-83/Mach64/PicoGUS rig came back PASS, delta +3.15fps
      vs. the pre-migration baseline of +3.0fps (threshold >=+2.0,
      A-to-A spread 0.0) -- the submodule migration changed neither
      build output nor measured performance. Steps 5-7 (tools/ DOSBox-X
      confs, tests/probes, .claude/agents/ charters) deliberately left
      doskutsu-local for now, per the plan's own recommendation --
      keeping tools/ local sidesteps the DOSKUTSU_ENVIRONMENT vs.
      DOS_PORT_ENVIRONMENT env-var mismatch rather than resolving it.
      upstream_revision per vendor/sources.manifest in the doskutsu
      repo as of this writing; re-check there for the current pin.
      upstream_license verified directly against doskutsu's own
      LICENSE file ("NOTE ON COMBINED BINARY" section) as of this
      writing.

  - name: adventure-game-studio
    upstream_url: https://github.com/adventuregamestudio/ags
    upstream_revision: null
    upstream_license: null
    upstream_license_verified: false
    port_repo_url: null
    language: C++
    original_sdl_version: "2"
    target_sdl_version: "3"
    graphics_api: software
    dos_status: BACKLOG
    priority: 1
    difficulty: 5
    dos_minimum_target: null
    dos_recommended_target: null
    maintainer: null
    notes: >
      Highest strategic value: one engine port potentially unlocks a large
      catalog of games. Needs an explicit SDL2->SDL3 strategy decision
      (direct port vs. compatibility shim) before kickoff -- see
      plans/SDL_DOS_PORTING_PROGRAM.md section 18. First deliverables are
      research-only (RESEARCH.md, SDL-SURFACE.md, DEPENDENCIES.md,
      PLATFORM-SURFACE.md, LICENSE-REVIEW.md, PLAN.md). License not yet
      verified -- read the actual LICENSE file in the pinned AGS revision
      before assuming anything.

  - name: vvvvvv
    upstream_url: https://github.com/TerryCavanagh/VVVVVV
    upstream_revision: null
    upstream_license: null
    upstream_license_verified: false
    port_repo_url: null
    language: C++
    original_sdl_version: "2"
    target_sdl_version: "3"
    graphics_api: software
    dos_status: BACKLOG
    priority: 2
    difficulty: 3
    dos_minimum_target: 486DX2-66
    dos_recommended_target: Pentium 75
    maintainer: null
    notes: >
      Flagship recognizable 2010 indie port. Licensing/distribution terms
      for source and assets must be reviewed (LICENSE-REVIEW.md) before any
      release packaging automation is built -- per
      plans/SDL_DOS_PORTING_PROGRAM.md section 7, VVVVVV's source has
      historically been distributed under source-available terms that are
      NOT automatically redistribution rights; do not assume "source
      available" means "freely redistributable" until the actual license
      terms are read and recorded here. Music decode cost likely exceeds
      render cost on a 486 -- plan a hardware-MIDI fallback.

  - name: meritous
    upstream_url: null
    upstream_revision: null
    upstream_license: null
    upstream_license_verified: false
    port_repo_url: null
    language: C
    original_sdl_version: "1.2"
    target_sdl_version: "3"
    graphics_api: software
    dos_status: BACKLOG
    priority: 3
    difficulty: 2
    dos_minimum_target: 486DX2-66
    dos_recommended_target: Pentium 75
    maintainer: null
    notes: >
      Recommended first "second game" port -- proves the shared/ platform
      and repeatable pattern with minimal engineering. Procedural
      dungeon-crawler, small resolution, plain C. Locate canonical upstream
      repo before starting.

  - name: openjazz
    upstream_url: https://github.com/OSSGames/GAME-SDL-openjazz
    upstream_revision: null
    upstream_license: null
    upstream_license_verified: false
    port_repo_url: null
    language: C
    original_sdl_version: "2"
    target_sdl_version: "3"
    graphics_api: software
    dos_status: BACKLOG
    priority: 4
    difficulty: 3
    dos_minimum_target: 486DX2-66
    dos_recommended_target: Pentium 75
    maintainer: null
    notes: >
      Native 320x200. An open SDL recreation of a game that originally ran
      on DOS, going back to DOS -- exercises 320x200 video behavior
      separately from doskutsu's 320x240 target. Asset licensing must be
      documented separately from the open engine.

  - name: powder
    upstream_url: null
    upstream_revision: null
    upstream_license: null
    upstream_license_verified: false
    port_repo_url: null
    language: C
    original_sdl_version: "1.2"
    target_sdl_version: "3"
    graphics_api: software
    dos_status: BACKLOG
    priority: 5
    difficulty: 2
    dos_minimum_target: 486DX2-66
    dos_recommended_target: 486DX2-66
    maintainer: null
    notes: >
      Low-risk confidence builder. Graphical roguelike, turn-based/low-
      frequency simulation, small sprite workload. Good test of keyboard
      input, save handling, and non-platformer graphics. Locate canonical
      upstream repo before starting -- confirm source availability and
      license terms as part of that search; unverified recollection is
      that POWDER's source has not always been openly licensed, which
      would block this candidate entirely if source access/modification
      rights don't actually exist. Do not proceed past research until this
      is confirmed one way or the other.

  - name: kobo-deluxe
    upstream_url: null
    upstream_revision: null
    upstream_license: null
    upstream_license_verified: false
    port_repo_url: null
    language: C++
    original_sdl_version: "1.2"
    target_sdl_version: "3"
    graphics_api: software
    dos_status: BACKLOG
    priority: 6
    difficulty: 3.5
    dos_minimum_target: Pentium 75
    dos_recommended_target: Pentium 75
    maintainer: null
    notes: >
      Renderer stress test, not a first port. Fast multidirectional
      shooter with heavy sprite/particle/scrolling load. Use an older
      pre-OpenGL-era revision. Locate canonical upstream repo before
      starting.

  - name: blob-wars
    upstream_url: null
    upstream_revision: null
    upstream_license: null
    upstream_license_verified: false
    port_repo_url: null
    language: C
    original_sdl_version: "1.2"
    target_sdl_version: "3"
    graphics_api: software
    dos_status: BACKLOG
    priority: 7
    difficulty: 3.5
    dos_minimum_target: Pentium 75
    dos_recommended_target: Pentium 75
    maintainer: null
    notes: >
      Closest workload to doskutsu/Cave Story of the candidates: full
      action/platformer, scrolling, multiple levels. Watch SDL_ttf and
      archive-format overhead. Target older SDL 1.x-era revisions before
      modernized branches. Locate canonical upstream repo before starting.

  - name: passage
    upstream_url: https://github.com/jasonrohrer/Passage
    upstream_revision: 2f713f261dc907f6feda106ebbee0464eeab791d
    upstream_license: public domain
    upstream_license_verified: true
    port_repo_url: https://forgejo.ecliptik.com/ecliptik/dossage
    language: C
    original_sdl_version: "1.2"
    target_sdl_version: "3"
    graphics_api: software
    dos_status: OPTIMIZING
    # 2026-08-31: real-hardware validated end to end on 486DX2-66/PicoGUS/
    # Cirrus CL-GD5430 (14.94fps, up from 13.992, KPI campaign closed
    # honestly short of a 15.000 target). Not RELEASE_READY yet: no
    # release/dist packaging step, no gallery/dossage/ entry, and Mach64
    # unverified against the current build (patch 0029 changed MAX_BPP,
    # which changes the VESA mode Mach64 negotiates) -- Cirrus and ViRGE
    # are validated, Mach64 is still an assumption. See dossage-cc's own
    # status update in this session's history for detail.
    priority: 8
    difficulty: 1
    dos_minimum_target: 486DX2-50
    dos_recommended_target: 486DX2-50
    maintainer: ecliptik
    notes: >
      Port repo is "dossage" (portmanteau, matches doskutsu's DOS+game
      naming convention; binary DOSSAGE.EXE). Tiny reference/skeleton port
      covering init/video/keyboard/timer/audio/filesystem/shutdown
      end-to-end -- intended to become the copy-paste starting skeleton in
      templates/ once ported. upstream_url/upstream_revision verified by
      cloning jasonrohrer/Passage directly (author's own GitHub account;
      only 2 commits, "Exported latest revision from sourceforge cvs" --
      this is the actual upstream, not a fork or mirror).
      upstream_license verified against the author's own project page
      (https://hcsoftware.sourceforge.net/passage/), which states
      "Placed in the public domain" -- no LICENSE file ships in the source
      tree itself, so the project homepage is the recorded verification
      source (quoted, with URL, in dossage's own LICENSE-REVIEW.md).
      Build dependency: Jason Rohrer's own minorGems utility library
      (https://github.com/jasonrohrer/minorGems), also public domain
      (verified via its own in-repo no_copyright.txt, dated 2018 -- an
      actual file in the pinned tree, stronger verification than
      Passage's own). Only a small subset of minorGems is used (file/path,
      string, settings, time, thread, sha1, TGA image decode, simple
      vector) -- the rest of that large multi-project library (portaudio,
      miniupnpc, network/p2p code, etc.) is irrelevant here and should not
      be vendored/built. Engine surface is unusually small even for
      difficulty:1: raw SDL 1.2 only, no SDL_mixer (custom sample-level
      software synth audio), no SDL_image (custom uncompressed .tga
      decoder in minorGems), no font library (bitmap numerals). Only
      Thread use is Thread::staticSleep() -- trivially compatible with the
      SDL3-DOS cooperative-scheduler / threading-zero invariant doskutsu
      established. Source-verified KPI: gameSource/game.cpp defines
      `int lockedFrameRate = 15;` -- the original design targets 15 fps,
      not Cave Story's 50 fps, so this port's KPI is ~15 fps sustained on
      the 486DX2-50/486DX2-66 tier rather than doskutsu's 25-30 fps bar.
      Unlike doskutsu, engine AND game assets are both public domain from
      the same author, so (unlike Cave Story's freeware-not-redistributed
      data) there is no legal blocker to shipping the assets in this repo
      -- LICENSE-REVIEW.md still documents this properly rather than
      skipping the paperwork because the answer is permissive.
      2026-08-26: BOOTSTRAP -> PLAYABLE in one session. SDL 1.2 -> SDL3
      migration (patches/passage/0001) plus DJGPP platform shims for
      minorGems' Path/Time/Thread (patches/minorgems/0001) compiled and
      linked clean on the first attempt after two small fixes (a DJGPP
      branch for minorGems' endian.h, and defining
      g_pixtone_active_count -- an engine-provided extern the shared
      SDL3-DOS Sound Blaster backend requires regardless of engine).
      Verified under DOSBox-X (build/stage, lfn=true pending an 8.3-
      rename follow-up -- several of Passage's own asset filenames
      exceed 8.3): title screen renders correctly and gameplay (maze
      corridor, player sprite, treasure chests) is visibly correct and
      responds to keyboard input. Full CPU/video/sound matrix sweep
      (doskutsu's own matrix) planned once optimized, per operator
      direction.
      2026-08-26 (later same day): found + fixed a real bug (missing
      SDL_INIT_AUDIO -- SDL3's SDL_OpenAudioDeviceStream doesn't
      self-init the audio subsystem the way SDL1.2's SDL_OpenAudio did),
      confirmed via temporary instrumentation that the synthesis math
      produces sane non-zero PCM (real audibility deferred to real-HW --
      DOSBox-X in this dev environment pre-buffers audio far ahead of
      real-time under cycles=max and never conclusively demonstrated
      playback). Completed the 8.3-rename follow-up
      (patches/passage/0002): all asset/settings filenames renamed,
      lfn flipped back to false, reverified. Attempted sdldos's
      g_pixtone_active_count -> g_dos_sfx_synth_active_count rename +
      submodule bump to 9e9c5a8, but that hub commit's SDL patch series
      fails to apply against this port's pinned SDL SHA -- reported,
      reverted, submodule held at bf10f15 pending a hub-side fix. Handed
      off to vcctrl for this port's first real-hardware pass.

  - name: supertux-0.1.x
    upstream_url: null
    upstream_revision: null
    upstream_license: null
    upstream_license_verified: false
    port_repo_url: null
    language: C++
    original_sdl_version: "1.2"
    target_sdl_version: "3"
    graphics_api: software
    dos_status: BACKLOG
    priority: 9
    difficulty: 4
    dos_minimum_target: Pentium 75
    dos_recommended_target: Pentium 90
    maintainer: null
    notes: >
      Pentium-class stretch target; a 486 port is a stretch goal, not the
      first objective. Only investigate early Milestone-era 0.1.x releases
      -- modern SuperTux carries a much heavier stack. 640x480 rendering
      raises framebuffer bandwidth concerns. Locate canonical upstream repo
      (and confirm which 0.1.x revision) before starting.
